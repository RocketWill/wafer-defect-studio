# Use PySide6 Widgets for the desktop UI

The product is a Windows desktop tool for algorithm engineers and will use PySide6 Widgets rather than Qt Quick/QML. Widgets provide a direct fit for dense engineering workspaces, dockable panels, tables, forms, and a QGraphicsView-based annotation canvas without introducing a second UI language and runtime model.
